# Fleet enrollment token
# Get this from Kibana → Fleet → Agent Policies → <Policy Name> → Add Agent
# This single agent policy should include both Logstash and PostgreSQL integrations
FLEET_TOKEN=your-fleet-enrollment-token-here
